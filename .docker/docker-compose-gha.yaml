services:
    hx:
        volumes:
            - source: ../_build

              target: /usr/local/src/hx/_build

              type: bind

            - source: ../deps

              target: /usr/local/src/hx/deps

              type: bind

            - source: ../

              target: /usr/local/src/hx

              type: bind
